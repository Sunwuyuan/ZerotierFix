<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with dynamic color support. -->
    <style name="Theme.ZerotierFix" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Enable dynamic color theming on Android 12+ -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">true</item>
        <!-- Customize your theme here. -->
        <item name="greyText">@color/grey_text_night</item>
        <item name="lightGreyText">@color/light_grey_text_night</item>
        <item name="tableRowGrey">@color/table_row_grey_night</item>
    </style>
</resources>